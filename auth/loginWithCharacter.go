package auth

import (
	"cats-industry-server/schema"

	"github.com/jmoiron/sqlx"
)

func loginWithCharacter(db *sqlx.DB, character *schema.Character) (err error) {
	// Prepare user

	// Link user with character if none exists

	return nil
}
